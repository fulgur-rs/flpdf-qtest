# qtest run artifacts (uploaded as CI artifacts, not committed)
survey/latest/

# Per-generation survey snapshots kept locally for cross-run diffs
survey/history/

# OS / editor scratch
.DS_Store
*.swp

# Python
__pycache__/
*.pyc
