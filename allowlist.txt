# flpdf-qtest allowlist
#
# Each non-empty, non-comment line declares an acceptance-test entry that
# MUST pass on CI. Format:
#
#   <test-stem>[:<subtest-name>]
#
# Examples:
#   deterministic-id                    # every subtest in deterministic-id.test must pass
#   arg-parsing:required argument       # only this single subtest is required to pass
#
# Semantics enforced by scripts/verify-allowlist.py:
#
#   allowlist PASS    → expected, verdict OK
#   allowlist FAIL    → REGRESSION, verdict FAIL, plotted on the trend chart
#   allowlist not run → verdict FAIL (typo, deleted upstream test, etc.)
#   non-allowlist PASS → allowlist-candidate (surfaced in qtest-summary.md)
#   non-allowlist FAIL → informational
#
# Verdict never gates CI: verify-allowlist.py exits 0 on FAIL as well, so a
# flpdf-side regression does not block the nightly metrics upload. The FAIL
# is instead visible on the trend chart as a bump in the `regressions`
# series. Only real errors (unparseable log, parse drift) exit non-zero.
#
# Policy: only add an entry AFTER the implementation in flpdf actually makes
# it pass. The allowlist is the public record of qpdf-CLI compatibility
# claims, not a wishlist.

# First real entry: linearize-pass1's subtest 1 ("linearize pass 1 file")
# asserts that `qpdf --linearize --static-id --compress-streams=n
# --linearize-pass1=b.pdf in.pdf a.pdf` exits 0 with empty stdout/stderr.
# Subtests 2 and 3 of the same .test compare bytes against qpdf's golden
# minimal-linearized.pdf / minimal-linearize-pass1.pdf and remain
# informational failures pending writer-side byte alignment.
# Implementation: flpdf-cli top-level qpdf-style flat flags (flpdf-d8a,
# merged as fulgur-rs/flpdf#69).
linearize-pass1:linearize pass 1 file

# Minimal promote batch from latest flpdf main survey (2026-05-18).
# These are single, named CLI behavior subtests with no duplicated subtest
# names in their suite-level context. Deliberately excludes repeated names
# such as encryption:check against base and broader JSON/output-comparison
# groups until the allowlist can address subtest identity more precisely.
copy-annotations:not keeping any fields
copy-annotations:page splitting with fields
merge-and-split:force full page duplication
merge-and-split:merge with . and implicit ranges
overwrite-self:output is not really output for split
rotate-pages:rotate all pages
split-pages:split pages %d at beginning

# Promoted 2026-05-19 after flpdf-4x6 (top-level `--static-id` warning
# suppressed to mirror qpdf). rotate-pages subtest 1 asserts
# `qpdf --static-id to-rotate.pdf a.pdf --rotate=...` exits 0 with empty
# stdout/stderr; it failed only on the spurious flpdf warning and now passes
# (current main, 2/2 stable). Same shape as `rotate-pages:rotate all pages`
# above; the byte-comparison subtest 2 ("check output") stays an
# informational failure pending writer alignment. Name is unique in
# rotate-pages.test, so the verifier's name-only match is unambiguous.
# Follow-up: flpdf-9kl (epic flpdf-vrn).
rotate-pages:page rotation
split-pages:split pages pdf extension

# Minimal promote batch from full survey at flpdf 23e71d9 (2026-05-27).
# Both entries have been passing in CI since at least flpdf 91080c7
# (2026-05-19) and exercise flpdf via the qpdf shim:
#   - json:check stream data (16, all) — file comparison subtest; the
#     compared files were produced by the qpdf shim → flpdf earlier in
#     json.test, so the byte equality is a real flpdf assertion.
#   - qpdf-json:json-output with file — invokes `qpdf --json-output=2
#     ... minimal.pdf a.json` (single command, exit 0, empty
#     stdout/stderr). Goes through shim → flpdf.
# Both names are unique within their .test files so the verifier's
# name-only match is unambiguous. Same shape and policy as the
# 2026-05-18 minimal batch above.
#
# Excluded from this batch: fix-qdf.test subtests. Despite passing
# locally on hosts where the qpdf package is installed, those tests
# invoke the standalone `fix-qdf` helper binary (not `qpdf`), which the
# shim does not intercept — so they exercise system /usr/bin/fix-qdf,
# not flpdf, and fail on CI which has no qpdf package.
json:check stream data (16, all)
qpdf-json:json-output with file

# Promote batch 2026-06-07, from full survey at flpdf 99e6d63 (2026-06-05).
# Each entry verified against THREE criteria (see memory
# feedback_allowlist_promote_criteria):
#   1. name uniqueness — subtest name occurs exactly once in its .test;
#   2. multi-survey stability — verify-allowlist expected_pass +1 with 0 new
#      regressions on the clean surveys 99e6d63 (2026-06-05) and 2a9bc92
#      (2026-05-29); the nine qpdf-only entries also hold on 23e71d9
#      (2026-05-27), giving three clean surveys;
#   3. qpdf-only — each subtest's own $td->COMMAND invokes `qpdf ...`
#      (→ shim → flpdf), not a host helper.
# (e34d70a from 2026-05-30 is a corrupt double-run survey — the same subtest
#  appears as both FAILED and PASSED, so even the pre-existing allowlist goes
#  red there; it is excluded as a baseline.)
decode-parameters:decode parameters empty list
decode-parameters:stream with indirect decode parms
decode-levels
encryption:create reference qdf
inline-images:complex inline image parsing
linearization:linearized and modified
linearization:modified
many-nulls:copy file with many nulls
preserve-unref:drop unused objects
qpdf-json:PDF to qdf

# encryption:decrypt — PR #229 `feat(cli): --decrypt`. Stable +1/0-regression
# on the two surveys where the feature exists (99e6d63, 2a9bc92); correctly
# absent on the pre-#229 survey 23e71d9 (feature absence, not instability).
# The subtest name is loop-generated from the filename, so re-check its match
# count stays 1 in future surveys before assuming it is permanently stable.
encryption:decrypt enc-XI-attachments-base.pdf

# Promote batch 2026-06-10, from full survey at flpdf cf9348d (FINDINGS-
# coverage-check-20260610.md). These eight were DEFERRED in the 2026-06-07
# batch above; cf9348d resolves both deferral reasons:
#   - cf9348d is now the THIRD clean adapter-shim survey (after 2a9bc92-fixqdf
#     2026-05-29 and 99e6d63 2026-06-05). In all three, raw harness.log shows
#     each of the eight at total=1 / PASS=1 / FAIL=0 — i.e. the loop-generated
#     names (page-without-contents per-$arg, encryption per-filename, fix-qdf
#     per-n) hold match-count == 1, the drift the 0607 batch was waiting on.
#   - fix-qdf 1 / 1 from pipe pass via the committed delegating adapter
#     shim/fix-qdf (commit c38828f) → `flpdf qdf-fix`, NOT host bleed: in the
#     same three surveys `fix-qdf 2` is FAIL=1 (flpdf lacks xref-stream QDF),
#     which would PASS if /usr/bin/fix-qdf were bleeding through. So these are
#     genuine flpdf assertions and safe on CI (which runs the same shim with
#     FLPDF_CLI_BIN). The 23e71d9 survey is excluded for fix-qdf — there
#     fix-qdf 2 PASSed (host bleed), a false positive.
# All eight: name unique in their .test; COMMAND is `qpdf ...` (→shim→flpdf)
# except the two fix-qdf entries (→adapter shim→flpdf). e34d70a (corrupt
# double-run) excluded as baseline as before.
basic-parsing:no qdf
coalesce-contents:coalesce to qdf
encryption:copy enc-XI-attachments-base.pdf
fix-qdf:fix-qdf 1
fix-qdf:fix-qdf 1 from pipe
page-without-contents:convert no contents ()
page-without-contents:convert no contents (--qdf)

# Promote batch 2026-07-25, from full survey at flpdf 1f037dd (FINDINGS-
# coverage-check-20260725.md). Ten qpdf-CLI compatibility subtests, each
# verified against ALL THREE criteria (feedback_allowlist_promote_criteria):
#   1. name uniqueness — raw harness.log match count == 1 (loop-generated
#      names verified per-name, not by .test grep alone);
#   2. multi-survey stability — verify-allowlist expected_pass +1 with 0 new
#      regressions on multiple clean surveys. Two groups:
#      A. Previously-ready 5 (031df92 × 2 + 1f037dd × 2 = 4 runs): the ones
#         flagged in FINDINGS-20260618 §6 as "waiting on second observation."
#      B. Long-stable 5 (99e6d63, cf9348d, d5c3dd3, 031df92, 1f037dd — 5+
#         clean surveys, all 1/1). Held back in prior batches only for
#         subtest-identity concerns which are now resolved by the per-log
#         match-count check.
#   3. qpdf-only — each subtest's own $td->COMMAND invokes `qpdf ...`
#      (→ shim → flpdf); no host helper, no grep/wc assertion.
# Sorted alphabetically by test stem (same convention as PR #16).
copy-annotations:complex copy annotations
encryption-parameters:linearize long-id.pdf
encryption-parameters:linearize short-id.pdf
encryption:create encrypted file
encryption:empty owner password
encryption:encrypt with AES
encryption:encrypt with V=5,R=6
form-xobject:overlay on page with no resources
incremental:handle delete and don't reuse
linearization:dump linearization: lin0

# Promote batch 2026-08-16, from full survey at flpdf 10a058f3 (FINDINGS-
# coverage-check-20260816-10a058f3.md). Thirteen qpdf-CLI-compatibility
# subtests, each re-verified against ALL THREE criteria directly against
# harness.log.fullsurvey-10a058f3 (feedback_allowlist_promote_criteria):
#   1. name uniqueness — (category, description) match count == 1 in the raw
#      harness.log, EXCEPT get-xref/parsed-offset which are 1-per-.test by
#      construction (two distinct descriptions, one ordinal each).
#   2. multi-survey stability — these have held expected_pass +1 with 0 new
#      regressions across many clean surveys since 2026-05-18/06-10/06-18
#      (character-encoding, replace-input, unicode-filenames, encryption,
#      filter-abbreviations, precheck-streams) and since 0812/0814
#      (get-xref, parsed-offset); reconfirmed again on 10a058f3.
#   3. qpdf-only / shim-routed — verified against vendor/qpdf-qtest/*.test:
#      character-encoding's three helper-binary subtests route through
#      shim/test_pdf_doc_encoding and shim/test_pdf_unicode (both hard-fail
#      if FLPDF_TEST_PDF_*_BIN is unset, so no host bleed-through is
#      possible); its fourth subtest and encryption:linearize-and-encrypt
#      call `qpdf ...` directly (→ shim/qpdf → flpdf); replace-input and
#      unicode-filenames route through shim/test_unicode_filenames;
#      filter-abbreviations:check output calls `qpdf-test-compare`
#      (→ shim/qpdf-test-compare → flpdf-test-compare); precheck-streams:
#      check output is a pure file comparison against a.pdf, which the
#      immediately preceding "bad stream" subtest in the same .test
#      produces via `qpdf --static-id ...` (both subtests PASS); get-xref
#      and parsed-offset route through shim/test_xref and
#      shim/test_parsedoffset respectively.
# Deliberately EXCLUDED from this batch (see below): decode-parameters:
# check file and preserve-unref:check output — both are duplicate-named
# across multiple ordinals in their .test, and at least one sibling ordinal
# FAILs on 10a058f3, so a single allowlist line would immediately register
# as a regression. page-without-contents:check output is also duplicate-
# named (3 ordinals) but all three currently PASS; held back out of caution
# pending verifier subtest-identity support, same as the loop-generated
# names below.
character-encoding:PDF doc encoding to Unicode
character-encoding:UTF-16 encoding
character-encoding:UTF-16 encoding errors
character-encoding:UTF-16LE strings
replace-input:create unicode filenames
unicode-filenames:create unicode filenames
encryption:linearize and encrypt file
filter-abbreviations:check output
precheck-streams:check output
get-xref:without object streams
get-xref:with object streams
parsed-offset:parsed offset without object streams
parsed-offset:parsed offset with object streams

# Promote batch 2026-08-16 (part 2), from the same full survey at flpdf
# 10a058f3 that produced the 13-entry batch above. Source: the 106
# candidates that went NEW in the 2026-08-15 survey (flpdf 88ef9efe,
# "scaffold test_driver test_2..test_98 (#839)"; see FINDINGS-coverage-
# check-20260815-e2937212.md), now at their 3rd clean build (criterion 2
# clear). Fifty-nine subtests promoted here, verified against ALL THREE
# criteria:
#   1. name uniqueness — (category, description) match count == 1 against
#      harness.log.fullsurvey-10a058f3 (computed programmatically, not by
#      .test grep alone). This excludes ~29 duplicate-named entries from
#      the 106 (e.g. attachments:check output ×2 with FAILing siblings,
#      basic-parsing:check output, encryption:extract attachments ×6,
#      form-xobject:overlay shrink/expand ×4 and overlay transformations
#      ×4, stream-replacements:check output — see the "Still EXCLUDED"
#      note below for the ones with a FAILing sibling specifically).
#   2. multi-survey stability — all 106 held expected_pass with 0
#      regressions across three consecutive clean surveys (e2937212
#      2026-08-15, ae38f887 and 10a058f3 both 2026-08-16).
#   3. qpdf-only / shim-routed — this batch is dominated by `test_driver N
#      <file>` invocations (→ shim/test_driver → flpdf), confirmed
#      individually against vendor/qpdf-qtest/*.test for every entry below;
#      a handful use `qpdf ...` directly or compare a file produced by an
#      immediately-preceding shim-routed subtest in the same .test (same
#      pattern as the already-allowlisted precheck-streams:check output).
# CRITICAL EXTRA FILTER applied on top of the three criteria: five of the
# 64 name-unique candidates invoke `test_driver N -` (stdin `-`), which is
# the exact CI-environment-dependent failure mode from PR #33 (non-
# interactive shell can't open `-` as a file) — cross-referenced against
# parity/qtest-11.9.0.jsonl by id "<suite> <ordinal>" and excluded because
# their manifest state is "applicable", not "passing": exceptions 2,
# object-handle-api 1, object-handle-api 4, tokenizer 5, type-checks 6.
# Passing locally does NOT mean passing on CI for this specific batch, so
# the manifest (CI-authoritative as of PR #33) is the source of truth here,
# not the local harness.log alone.
# fix-qdf:fix-qdf 2 / fix-qdf 2 from pipe / fix-qdf with big object stream
# get special mention: they were EXCLUDED in the 2026-06-10 batch below for
# a real flpdf gap (qdf-fix rejected xref-stream/object-stream QDF). They
# started passing with e2937212 (2026-08-15) and have held stably since;
# fixture inspection confirms fix2.qdf/big-ostream.pdf are genuine xref-
# stream/object-stream content, not weakened comparisons, so this is a
# real flpdf capability fix, not a shim artifact. fix-qdf:identity fix-qdf
# 2 is NOT included — duplicate name (ordinals 4 and 8), deferred with the
# other duplicate-name groups.
attachments:attachments
attachments:list attachments verbose
attachments:remove attachment (test_driver)
attachments:show attachment
cleartext-metadata:check metadata: compressed-metadata.pdf
cleartext-metadata:check metadata: enc-base.pdf
copy-foreign-objects:copy objects error
copy-foreign-objects:indirect filters
custom-pipeline:check output
custom-pipeline:output to custom pipeline
disable-filter-on-write:no filter on write
disable-filter-on-write:check output
encryption:copy encryption parameters
encryption:set encryption before set filename
final-version:check final version
fix-qdf:fix-qdf 2
fix-qdf:fix-qdf 2 from pipe
fix-qdf:fix-qdf with big object stream
form-xobject:form xobject creation
form-xobject:page operations on form xobject
interactive-form:button fields
interactive-form:form test: minimal
many-nulls:copy sparse array
name-number-trees:nntree erase
name-number-trees:nntree split
object-copying:copy a stream
object-copying:shallow copy an array
object-handle-api:equality
object-stream:overridden compressed objects
outlines:outlines: outlines-with-actions
outlines:outlines: outlines-with-loop
outlines:outlines: outlines-with-old-root-dests
outlines:outlines: page-labels-and-outlines
page-api:basic page API
page-api:delete and re-add a page
page-api:get box methods
page-api:manual page manipulation
page-labels:complex page labels
page-labels:no page labels
page-labels:no zero entry for page labels
pages-tree:extract duplicated pages
pages-tree:linearize duplicated pages
parsing:extra header text
parsing:terminate parsing
parsing:tokenize content streams
pclm:check output
pclm:write as PCLm
precheck-streams:bad stream
qpdf-json:simple version of writeJSON
qpdf-json:write JSON to pipeline
signature-dictionary:checkout output (remove restrictions)
specific-file:pages points to page
stream-line-terminators:check output
stream-replacements:add page contents
stream-replacements:new streams
stream-replacements:replace stream data
stream-replacements:replace stream data compressed
token-filters:check output
token-filters:token filter
type-checks:compound type checks

# Still deliberately EXCLUDED (re-evaluate in a later survey):
#   - decode-parameters:check file / preserve-unref:check output —
#     duplicate subtest name across ordinals in the same .test, with at
#     least one sibling ordinal FAILing on 10a058f3 (decode-parameters
#     ordinal 6, preserve-unref ordinals 4 and 6). A single allowlist line
#     matches every same-named ordinal and would go red immediately.
#   - page-without-contents:check output — duplicate name across 3
#     ordinals, all currently PASS, but no per-ordinal identity in the
#     verifier yet; deferred with the other duplicate-name groups below.
#   - encryption:make sure there is no xref stream — its own COMMAND is
#     `grep /ObjStm b.pdf | wc -l`, a host grep/wc assertion, NOT a qpdf call,
#     so passing it is not an flpdf-CLI compatibility claim (criterion 3).
#   - fix-qdf:identity fix-qdf 2 — DUPLICATE name (ordinals 4 and 8, both
#     currently PASS); fix-qdf:identity fix-qdf 1 is likewise a duplicate
#     (appears in both the direct and pipe loops). Both stay excluded
#     pending verifier subtest-identity support.
#   - exceptions:check certain exception types / object-handle-api:
#     dictionary keys / object-handle-api:isScalar checks / tokenizer:
#     quoted char edge cases / type-checks:indirect objects belonging to
#     destroyed QPDF — all invoke `test_driver N -` (stdin `-`); PASS
#     locally but FAIL on CI's non-interactive shell (PR #33's known
#     issue). parity manifest marks these `applicable`, not `passing`.
#   - qpdf-json:good:* — loop-generated subtest names; match counts not yet
#     re-confirmed stable, deferred pending a per-name count check.
#   - encryption:check against base ×28, unicode-password:* repeats,
#     attachments/basic-parsing/encryption:extract attachments/form-xobject
#     duplicate "check output"-style names from the 0815 batch — a single
#     allowlist line would match every instance and go red on any one FAIL
#     (several of these already have a FAILing sibling ordinal). Needs
#     verifier subtest-identity support (line/index).
